# Bandit config moved to pyproject.toml [tool.bandit].
# Run :   bandit -r backend cli.py
#
# Pas besoin de `-c pyproject.toml` : tous les faux positifs B608 sont annotés
# `# nosec B608` à la ligne, avec un commentaire explicatif au-dessus du SQL.
# Bandit default invocation est clean (0 issues).
